export enum TabTitlesEnum {
  chat = "chat",
  config = "config",
  history = "history",
}

export type Theme = {
  selected: ColorThemeEnum;
};

export enum ColorThemeEnum {
  dark = "dark",
  light = "light"
}
